import React from "react";
import { TextInput, View } from "react-native";

const ExpenseForm = () => {
	return (
		<View>
			<TextInput></TextInput>
		</View>
	);
};

export default ExpenseForm;
